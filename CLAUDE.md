# AI-Debuggable Logging

让 AI 调试时能看到运行时实际发生了什么，而不是靠猜。日志面向 AI 诊断，不追求运维完整性。

---

**先读日志，再动代码**

遇到 bug 先看 `logs/YYYY-MM-DD_HH-MM/`（按启动时间建文件夹，按模块分文件）。没有日志就先加日志复现，禁止盲猜。修完确认该错误不再出现。

**新模块多记，稳定后只记错**

首次接入时，关键环节都加日志（入口参数、中间状态、外部调用、分支判断），当作给 AI 写的运行说明书。连续运行无新错误即算稳定，此时降级或删除多余 info 日志。稳定后只在错误路径写日志，正常路径保持安静——噪音比没有日志更糟。

**一行 JSON，固定字段**

每条日志一行 JSON，示例：`{"ts":"2024-01-01T00:00:00Z","level":"error","mod":"auth","msg":"token expired","err":"<stack>"}`。必填：`ts`（ISO 8601）、`level`、`mod`、`msg`。error 带 `err`（堆栈），外部调用失败带 `req`（输入）。不写自由文本。

**够用就行，不搞框架**

一个写 JSONL 的函数，不超过 20 行，函数内部按启动时间自动创建日期目录。遇到 bug 顺手加，不要一次性全加。日志目录放 `.gitignore`。
